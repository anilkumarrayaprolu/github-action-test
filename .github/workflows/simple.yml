name: First-workflow script

on: [push]


jobs:
  run-shell-commands:
    runs-on: ubuntu-latest
    steps:
      - name: echo a string
        run: echo "first project"
      - name: multiline command
        run: |
          node -v
          npm -v